# Password-Server
This program simulates a simple password server using a hash table as the underlying data structure. The hash table used in the password server program is from a self implementation of a hash table class template. 
